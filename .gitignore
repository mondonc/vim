# Plugin directories (managed by vim-plug / lazy.nvim)
bundle/
autoload/plug.vim

# Undo history
undodir-vim/
undodir-nvim/

# Swap / backup (disabled but just in case)
*.swp
*.swo
*~

# AI flag (local only, created by ./install.sh ia)
.ai-enabled
.ai-model
